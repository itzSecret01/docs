---
title: "Quickstart — CreeperCloud FiveM Server Setup"
description: "Setup your FiveM Server! "
---

## 

## 🇺🇸 How to Create Your Server on CreeperCloud

To get started with your FiveM server, follow these steps on [CreeperCloud](https://creepercloud.io/):

## Step 1: Access the Website

- Go to [CreeperCloud.io](https://creepercloud.io/)- Log into your account or register if you haven't yet.
- 

## Step 2: Create Your Server

In the panel, click **"Create Server"** and fill in the form with the following:

| Field              | Example Value       | Notes                            |

|-------------------|---------------------|----------------------------------|

| **Name**          | `MyFiveMServer`     | Choose a recognizable name.      |

| **CPU (%)**       | `345` (100% minimum) | You have up to 5880% available. |

| **Memory (RAM)**  | `7074` MB           | Recommended based on players.    |

| **Disk (Storage)**| `65020` MB          | Allocate enough space.           |

| **Additional Ports** | `0`              | Optional unless needed.          |

| **Backups**       | `0`                 | Configure if you want snapshots. |

### Step 3: CPU, RAM and Storage Recommendations

Here’s what we recommend depending on the number of players:

| Players     | CPU (%) | RAM (MB) | Disk (MB) |

|-------------|---------|----------|-----------|

| 0–20        | 100–200 | 2048–4096 | 30000\+    |

| 20–50       | 200–400 | 4096–8192 | 50000\+    |

| 50–100\+     | 400–800 | 8192–16000 | 70000\+   |

\> ⚠️ **Always allocate at least 100% CPU for FiveM servers**, even for small projects.

---

## 🇪🇸 Cómo crear tu servidor en CreeperCloud

Para comenzar con tu servidor de FiveM, sigue estos pasos en [CreeperCloud](https://creepercloud.io/):

### Paso 1: Entra a la web

- Visita https://creepercloud.io/
- Inicia sesión o regístrate si no tienes cuenta.

### Paso 2: Crear tu servidor

En el panel, haz clic en **"Create Server"** y rellena el formulario con lo siguiente:

| Campo              | Valor de ejemplo     | Comentario                          |

|--------------------|----------------------|-------------------------------------|

| **Nombre**         | `MiServidorFiveM`    | Usa un nombre reconocible.          |

| **CPU (%)**        | `345` (mínimo 100%)  | Tienes hasta 5880% disponible.      |

| **Memoria (RAM)**  | `7074` MB            | Según número de jugadores.          |

| **Disco (Almacenamiento)** | `65020` MB | Asegúrate de tener espacio suficiente. |

| **Puertos adicionales** | `0`            | Opcional si no se requieren extras. |

| **Copias de seguridad** | `0`            | Puedes configurar backups si deseas.|

### Paso 3: Recomendaciones para CPU, RAM y almacenamiento

Según la cantidad de jugadores, recomendamos lo siguiente:

| Jugadores   | CPU (%) | RAM (MB) | Disco (MB) |

|-------------|---------|----------|------------|

| 0–20        | 100–200 | 2048–4096 | 30000\+     |

| 20–50       | 200–400 | 4096–8192 | 50000\+     |

| 50–100\+     | 400–800 | 8192–16000 | 70000\+    |

\> ⚠️ **Siempre debes asignar al menos un 100% de CPU a tu servidor FiveM**, incluso si es solo para pruebas.

---

**Author:** itz_Secret

**Discord:** `itz_Secret`